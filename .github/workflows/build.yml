# Disabled on fork - use build-snapshot.yml instead
name: Build Paper (disabled)
on:
  workflow_dispatch:

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is disabled on this fork. Use build-snapshot.yml instead."
